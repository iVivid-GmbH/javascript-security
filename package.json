{
  "name": "javascript-security-reference",
  "version": "1.0.0",
  "description": "Complete JavaScript & Frontend Security Reference — Slidev presentation + 45 deep-dive markdown files",
  "private": true,
  "scripts": {
    "dev": "slidev slides.md --open",
    "build": "slidev build slides.md --base /javascript-security/",
    "export:pdf": "slidev export slides.md --format pdf --output dist/presentation.pdf",
    "export:png": "slidev export slides.md --format png --output dist/slides/",
    "preview": "vite preview"
  },
  "dependencies": {
    "@slidev/cli": "^0.49.0",
    "@slidev/theme-default": "^0.25.0"
  },
  "devDependencies": {
    "playwright-chromium": "^1.40.0"
  }
}
